// pega o conteúdo da tag h1 e aplica uma condição
let titulo = document.getElementById("titulo").innerText;

if (titulo == "Javascript") {
  alert("Sucesso!");
  alert(titulo);
}

confirm("Escolha uma das duas opções");
